import actionType from "./actions";

const initialState = {
  ingredients: null,
  totalPrice: 6.9,
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
